[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "pysimplesoap"
version = "1.16.1"

[tool.setuptools]
py-modules = ['pysimplesoap']
